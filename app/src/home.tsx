import { Button } from "@mui/material";

export const Home = () => (
  <>
    <Button variant="contained">Nice button, dude</Button>
    <br />
    <Button variant="contained" color="secondary">
      Nice button, dude
    </Button>
  </>
);
